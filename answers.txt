What is the difference between a merge and a rebase?

Git merge allows for the preservation of complete project history and does not re-write public commits. While Git rebase frees us from having unnecessary merge commits. It also maintains a linear history and preferably used on private branches.

What is a remote repository?
Remote repositories are usually hosted on the Internet and is where all your code is stored, including all the different branches, versions of your project, and changes.